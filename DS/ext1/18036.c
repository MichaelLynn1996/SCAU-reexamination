#include<stdio.h>
#define MAX 1000000

struct person
{
    int num;
    struct person *next;
};

struct person *createLink(int n)
{
    struct person *head, *temp;
    head = ()
}

int main(int argc, char const *argv[])
{
    int n;
    scanf("%d", &n);
    createLink(n)

    return 0;
}
